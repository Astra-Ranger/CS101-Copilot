<!-- Page 1 -->

# 实验-计算机科学导论-课件12

# 个人作品-1

## 静态网页

大湾区大学

---

<!-- Page 2 -->

# <span style="color:#2b0057;">个人作品实验目标</span>

- 本实验将会以 “王小二” 编写一个网页博客介绍他发明了网页版时钟和计算器的伟大事迹为例，讲述相关的编程知识
- 使用创造性的表达制作<span style="color:red;">动态</span>网页
- 完成本实验需要同学们展示<span style="color:red;">自学</span>的能力
- 每位同学需要完成并<span style="color:red;">向全班展示</span>：
  - 一个<span style="color:red;">动态</span>网页，包含<span style="color:red;">HTML, CSS, JavaScript</span>代码
  - 该网页展示了你的<span style="color:red;">创造力</span>

---

<!-- Page 3 -->

# <span style="color:#2b005b;">个人作品实验安排</span>

<table>
  <thead>
    <tr>
      <th>周次</th>
      <th>日期</th>
      <th>实验课</th>
      <th>课程内容</th>
      <th>作业提交内容</th>
      <th>说明</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="center">12</td>
      <td align="center">12/5</td>
      <td align="center">制作静态网页</td>
      <td>HTML结构<br>HTML语法</td>
      <td></td>
      <td rowspan="3" align="center">
        需提交作品名称、包含<br>
        <code>index.html</code>的<code>zip</code>文件<br>
        最终得分以最终作品为准<br>
        截止时间前均可反复提交
      </td>
    </tr>
    <tr>
      <td align="center">13</td>
      <td align="center">12/12</td>
      <td align="center">制作网页版<br>计算器与时钟</td>
      <td>
        修改计算器的结果<br>
        读取操作数<br>
        执行选中的运算<br>
        添加静态时钟<br>
        让时钟动起来
      </td>
      <td align="center"><span style="color:#0070c0;">12/18 17:00 第一次提交</span></td>
    </tr>
    <tr>
      <td align="center">14</td>
      <td align="center">12/19</td>
      <td align="center">交流分享</td>
      <td>分享个人作品设计</td>
      <td align="center"><span style="color:red;">12/25 17:00 最终作品</span></td>
    </tr>
    <tr>
      <td align="center">15</td>
      <td align="center">12/26</td>
      <td align="center">网页互评</td>
      <td>小组讨论与互评</td>
      <td></td>
      <td></td>
    </tr>
  </tbody>
</table>

---

<!-- Page 4 -->

# 评分标准

- 创造性
  - **0分**：无创意，完全依赖模板或已有内容，无任何创新或个性化表达，作品完全由**AI**生成。
  - **1分**：创意欠缺，偶有微小改动，但整体仍以模仿为主，尝试加入少量新元素，但效果不明显或逻辑牵强，核心功能由**AI**设计实现。
  - **2分**：具备基础创新性，结合现有方法或内容，有一定重组或改进。
  - **3分**：较有创意，内容或设计具有独特性和启发性。
  - **4分**：很有创意，且体现了精心的设计与实现。

---

<!-- Page 5 -->

# 评分标准

- 完成度
  - **0分**：未完成，无法展示核心功能，或存在抄袭，使用他人材料无致谢。
  - **1分**：完成度低，有严重缺陷，仅能展示少部分功能，使用他人材料或代码比例过大。
  - **2分**：基本完成，有bug，能展示主要功能，多处使用他人资源但致谢规范。
  - **3分**：完成，仅有少量bug，几乎不影响展示功能，使用少量外部资源且致谢规范。
  - **4分**：完成度高，没有bug，功能展示完整，几乎完全原创，致谢规范。

---

<!-- Page 6 -->

# 作品提交

- 最终截止时间：**2025 年 12 月 25 日 17:00 前**
- 提交内容：作品名称 + `zip`压缩包
  - 在 `https://cs101.gbu.edu.cn/part` 的作品提交界面提交包含 `index.html` 的 `zip` 压缩包及作品名称

---

## 作品提交界面说明

标签页：

- 作业说明
- 历史作品
- 作业提交

### 个人作品实验

注意：直接选中包含 `index.html` 在内的所有文件，右键压缩为 `zip`。不要把 `index.html` 放在文件夹里，然后压缩文件夹。否则无法在平台上预览，也无法被评分。

1. 点击“作业提交”

提交轮次选择：`1`

提交表单：

- 作品名称：测试
- 上传文件：`test.html.zip`

压缩包大小不超过 `100MiB`  
解压后单个文件大小不超过 `100MiB`

将文件拖到此处或点击以选择文件  
文件大小不应超过 `100MiB`

按钮：提交

2. 选择提交轮次，填写作品名称，上传 `zip` 压缩包，点击“提交”

6

---

<!-- Page 7 -->

# 参考

- 在线教程
  - <https://www.w3schools.com/>
  - <https://www.freecodecamp.org/chinese/learn>
- 实验参考示例与部分往年作品
  - <https://cs101.gbu.edu.cn/part>

---

<!-- Page 8 -->

# 注意事项

- 本实验不允许使用Vue，React等外部框架，**外部代码占比<10%**
- 致谢以文字的形式在页面底部显示（而不是注释）
  - 如果引用外部图片等资源，必须明确指明图片等资源链接
- 标注创造性：在`<head>`部分以注释的形式说明创造性，且需说明哪些部分为**AI**参与，使用了哪些**AI**
  - 例如，按钮的css样式由Deepseek v3生成，某某bug在Qwen的帮助下修复等
- 本次实验重在进行“**创造性表达**”
  - 请从大班课20个题目中进行选题，并进行个人创造性表达

---

<!-- Page 9 -->

# 例子

## 通过傅里叶变换绘制果壳和ಠ_ಠ?

旋转向量个数(1~1000): `1000`  
线条粗细: `3`  
绘制速度: `5`  
绘制帧率: `60`

更改图案: `I ♥ 果壳`  
对原理感兴趣? `来一点数学!`

该演示会在每一个时刻计算一个傅里叶级数的复数值，并将其绘制在下面的画布上（x坐标表示实部，y坐标表示虚部），每一个时刻都对应“果壳曲线”上的一个点。  
尝试更改旋转向量的数目，看看该傅里叶级数在更少项数（更少信息）的情况下曲线会损失哪些细节，又呈现了什么样的基本特征?

[通过傅里叶变换绘制果壳和ಠ_ಠ?]()

9

---

<!-- Page 10 -->

# 例子-编程习惯好

- 此网页展示了斐波那契亚曲线
- 遵循了良好的编程习惯

**Fibonacci Curve**

![Fibonacci Curve](Fibonacci.html)

[Fibonacci.html](Fibonacci.html)

10

---

<!-- Page 11 -->

# 例子-编程习惯好

```javascript
// code executed directly:
var len = 14; // the number of Fibonicaii numbers to form the Curve (Global variable)
var fibonacciArray = new Array(len); // Fibonicaii numbers to form the Curve
calculate_fibnacii();
drawFibonacciArc();
// function definition:
function calculate_fibnacii(){
    fibonacciArray[0] = 1; fibonacciArray[1] = 1;
    for(var i=2;i<len;i++) fibonacciArray[i]=fibonacciArray[i-1]+fibonacciArray[i-2];
}
function drawFibonacciArc(){
    // show the whole curve by draw many 1/4 circles
    var canvas = document.getElementById("myCanvas"); // get Canvas
    var ctx = canvas.getContext("2d");
    var x0 = 400, y0 = 200;
    var x = x0, y = y0, startDegree = 0, endDegree = 90;

    for (var i = 0; i < len; i++) {
        // Draw 1/4 Circle whose radius is fibonacciNumber[i]
        var radius = fibonacciArray[i];
        ctx.beginPath();
        ctx.strokeStyle = "black";
        ctx.arc(x,y,radius,(startDegree/180)*Math.PI,(endDegree/180)*Math.PI);
        ctx.stroke();
        // update the position of the center of next 1/4 circle
        var direction = startDegree/90;
        if(i<len-1){
            var inc_x = (direction%2-2*Math.floor(direction/3))*(fibonacciArray[i+1]-fibonacciArray[i]);
            var inc_y = -(1-direction+2*Math.floor(direction/3))*(fibonacciArray[i+1]-
fibonacciArray[i]);
            x += inc_x; y += inc_y;
        }
        // update the range of degrees of next 1/4 circle
        startDegree = (startDegree+90)%360;
        endDegree = (endDegree+90)%360;
    };
}
```

- 把常数的定义放在前面
- 使用描述性的变量名
- 避免重复的代码
- 使用注释
- 避免“魔数”

---

<!-- Page 12 -->

# 个人作品-1（4学时）

- 本周目标
  - 掌握HTML结构及语法
  - 学习如何修改计算器的结果、读取操作数、执行选中的运算
- 学时安排
  - **学时1：** HTML结构及语法
  - **学时2：** 添加小标题、添加名字和时间、进度表、动机、计算器
  - **学时3：** 块级元素和内联元素、添加元素的样式、给元素添加属性
  - **学时4：** 交流讨论，构思自己的个人作品

---

<!-- Page 13 -->

# 本节课将实现的网页：

## 计算器和时钟养成记

作者：**_王小二_*** 时间：_2020年02月26日_

### 发明前

在发明前，我也不知道自己能不能做成。

列表：王小二的动机

1. 学习CSS
2. 学习HTML
3. 学习JS
4. 写计算器和时钟

### 发明中

过程中，查了好多资料，好累但很快乐。下表是我这几天的艰辛历程：

表：王小二艰难历程表

| 事件 | 感悟 |
|---|---|
| 看CSS语法 | 一天没睡 |
| 看HTML语法 | 还是没睡 |
| 看JS语法 | 还是GO香 |

### 发明后

终于做好了，虽然很粗糙，那也是亲儿子

计算器：

操作数1：<input type="text" />

操作数2：<input type="text" />

<input type="radio" /> 加 <input type="radio" /> 减 <input type="radio" /> 乘 <input type="radio" /> 除 <input type="radio" /> 模

结果：xxxx

<button>计算</button>

index.html

---

<!-- Page 14 -->

# 1. HTML 结构

- HTML (HyperText Markup Language)
  - 超文本标记语言，用来描述网页上有什么

```html
<html>
      <head>
      一些代码
      </head>
      <body>
      一些代码
      </body>
</html>
```

← 所有 HTML 代码都需要遵循这一结构

注意 `'/'` 的存在!

---

<!-- Page 15 -->

# HTML结构-例子

- 所有的**html**源代码文件名均以 **.html** 为扩展名
  - 新建**index.html**文档，并写入下面的代码
  - 使用浏览器（建议**Chrome**）打开该文档

```html
<html>
    <head>
        <meta charset="UTF-8" >
        <title>美好的一天</title>
    </head>
    <body>
        <h1>计算器和时钟养成记</h1>
    </body>
</html>
```

`<title>美好的一天</title>`

浏览器标签页标题：美好的一天

页面标题：计算器和时钟养成记

`<h1>计算器和时钟养成记</h1>`

---

<!-- Page 16 -->

# 开发方式1： 使用编程平台开发

- 在编程平台上新建`index.html`文件并写入代码
- 输入命令 `python3 -m http.server`，在弹出窗口选择 “在浏览器打开”

```terminal
coder@13cdb3755dc:~/project$ ls
1.input   autumn.bmp       example.py       index.html   server.go   tmp
1.usr     dark_panda.bmp   hide             mask.py      static
2.py      dark_panda.py    hongloumeng.txt  panda.bmp    test.py

coder@13cdb3755dc:~/project$ python3 -m http.server
Serving HTTP on 0.0.0.0 port 8000 (http://0.0.0.0:8000/) ...
```

> NOTIFICATIONS  
> Your application running on port 8000 is available. See all forwarded ports  
> **Open in Browser 在浏览器中打开**

16

---

<!-- Page 17 -->

# 开发方式2：本地开发

- 在本地下载并安装**VSCode**软件
- 在本地创建一个新文件夹，使用**VSCode**打开该文件夹
- 在文件夹中新建`index.html`文件，并使用**VSCode**编辑
- 在“文件资源管理器”找到创建的`index.html`，右键打开方式选择浏览器（推荐使用**Chrome**），可以看到网页效果

---

<!-- Page 18 -->

# HTML结构-例子

- 所有的`html`源代码文件名均以 `.html` 为扩展名
  - 新建`index.html`文档，并写入下面的代码
  - 使用浏览器（建议`chrome`）打开该文档

```html
<html>  html起始标签
    <head>  head起始标签
        <meta charset="UTF-8" >    meta标签： 指明文本的编码方式
        <title>美好的一天</title> —对title标签:中间为网页的标题（名字）
    </head>  head 结束标签
    <body>  body 起始标签
        <h1>计算器和时钟养成记</h1>  h1标签： 显示在网页中的文字
    </body>  body结束标签
</html> html结束标签

---

<!-- Page 19 -->

# HTML语法-概述

- **标记语言：** HTML是一种标记语言，它由一个个标签组成，例如：
  - `<html>`、`</html>`
  - `<head>`、`</head>`
  - `<body>`、`</body>`
- **标签之间允许嵌套：** 即一对标签之间可以定义另一对标签
  - 上例中，`<html>…</html>`之间定义了`<head>…</head>`和`<body>…</body>`
  - `<head>…</head>`之间定义了`<meta>`和`<title>`
  - `<body>…</body>`之间定义了`<h1>…</h1>`
- **与Python不同：**
  - Python作为一种编程语言，常常包含对数字符号的各种操作；
  - 而HTML只是用来指定一个网页上显示什么内容

---

<!-- Page 20 -->

# HTML语法-基本概念

- **标签：** HTML标签是一个形如 `"<keyword>"` 的单词，如 `<html>`
- 大部分HTML标签都按照 `<keyword>…</keyword>` 的模式
  - 标签成对出现，如 `<h1>…</h1>`
  - 前面的 `<h1>` 称为起始标签（开放标签），后面的 `</h1>` 称为结束标签（闭合标签，注意：以 `/` 开头）
- **属性：** HTML起始标签可以带有属性
  - 属性的值是用引号括起来的字符串，其语法如下：

    ```html
    <h1 attribute_name="…">…</h1>
    ```

  - **元素：** `"<key_word>…</key_word>"` 这一整体称为一个HTML元素

示意：

```text
标签 → <h1>计算器和时钟养成记</h1> → 元素

---

<!-- Page 21 -->

# HTML语法-`<head>`中的元素

- 一对`<head>…</head>`中间一般都需要一个`<meta>`标签和一对`<title>`标签

```html
<head>
    <meta charset="UTF-8">
    <title>文本</title>
</head>
```

名字空间：浏览器允许的字符串（中文、英文和空格，多个空格会合并成一个）

| 标签 | 含义 |
|---|---|
| `<meta charset="UTF-8">` | 规定使用 utf-8 编码 |
| `<title>…</title>` | 规定整个网页的标题(名字) |

---

<!-- Page 22 -->

## 2. 添加小标题

- 王小二觉得要介绍自己的创建过程，他的文章要分为三个部分，取三个标题（显示在网页中），分别为：“发明前”，“发明中”和“发明后”

```html
<html>
<head>
    <meta charset="UTF-8">
    <title>美好的一天</title>
</head>
<body>
    <h1>计算器和时钟养成记</h1>
    <h2>发明前</h2>
    <h2>发明中</h2>
    <h2>发明后</h2>
</body>
</html>
```

网页显示：

# 计算器和时钟养成记

## 发明前

## 发明中

## 发明后

- `<h1>文本</h1>`
- `<h2>文本</h2>`
- `<h3>文本</h3>`
- `<h4>文本</h4>`
- `<h5>文本</h5>`
- `<h6>文本</h6>`

字体逐渐变小

22

---

<!-- Page 23 -->

# 添加自然段-段落、换行、水平线、注释

- 王小二在添加小标题之后，开始兴高采烈地着手写每一个小标题的下面的段落，并且希望每一个小段之间尽可能空开一些。

```html
<body>
    <h1>计算器和时钟养成记</h1>
    <h2>发明前</h2>
    <p>在发明前，我也不知道自己能不能做成。</p>
    <br>
    <hr>
    <h2>发明中</h2>
    <p>过程中，查了好多资料，好累但很快乐。</p>
    <br>
    <hr>
    <h2>发明后</h2>
    <p>终于做好了，虽然很粗糙，那也是亲儿子</p>
</body>
```

# 计算器和时钟养成记

## 发明前

在发明前，我也不知道自己能不能做成。

---

## 发明中

过程中，查了好多资料，好累但很快乐。

---

## 发明后

终于做好了，虽然很粗糙，那也是亲儿子

| 说明 | 语法 |
|---|---|
| 段落 | `<p>文本</p>` |
| 换行 | `<br/>`（或者`<br>`） |
| 水平线 | `<hr/>`（或者`<hr>`） |
| 注释 | `<!--注释内容-->` |

---

<!-- Page 24 -->

# 添加名字和时间-文本格式

- 王小二在添加段落之后，想要在自己的博客上添加名字和书写时间。名字希望斜体加粗并且上标一个小星星，时间希望是斜体。
- 需要用到的知识为<span style="color:red">文本格式化标签</span>：
  - 文本格式化标签通常是一对标签，这对标签指定了标签中间的文本的格式

```html
<body>
    <h1>计算器和时钟养成记</h1>
    作者： <b><i>王小二</i></b>
<sup>*</sup> 时间： <i>2020年02月26日
</i>

    <h2>发明前</h2>
    ...
    ...
</body>
```

## 计算器和时钟养成记

作者： ***王小二***<sup>*</sup> 时间： *2020年02月26日*

## 发明前

- 加粗：`<b>加粗文本</b>`
- 斜体：`<i>斜体文本</i>`
- 上标：`<sup>上标文本</sup>`
- 下标：`<sub>下标文本</sub>`

---

<!-- Page 25 -->

# 添加进度表-表格

- 王小二在添加完姓名和时间之后，开始回忆自己的创作过程，希望用一个表格的方式记录自己这几天干的事情。
  - 用一对 `<table>...</table>` 定义表格；
  - 在这对标签中间，用 $n$ 对 `<tr>...</tr>` 定义 $n$ 行；
  - 在每对 `<tr>...</tr>` 之间，用 $m$ 对 `<td>...</td>` 在同一行定义 $m$ 个单元格；
  - 一对 `<td>...</td>` 中间的文本就是显示在单元格中的内容

```html
<table>

        <tr><td>(row0,col0)</td><td>(row0,col1)</td></tr>

        <tr><td>(row1,col0)</td><td>(row1,col1)</td></tr>

</table>

---

<!-- Page 26 -->

# 添加进度表-表格

- 王小二在添加完姓名和时间之后，开始回忆自己的创作过程，希望用一个表格的方式记录自己这几天干的事情。

```html
<table border="1">  → 表格框线条粗细
    表： 王小二艰难历程表
    <tr>
        <td>事件</td><td>感悟</td>
    </tr>
    <tr>
        <td>看CSS语法</td><td>一天没睡</td>
    </tr>
    <tr>
        <td>看HTML语法</td><td>还是没睡</td>
    </tr>
    <tr>
        <td>看JS语法</td><td>还是GO香</td>
    </tr>
</table>
```

## 发明中

过程中，查了好多资料，好累但很快乐。

表：王二小艰难历程表

| 事件 | 感悟 |
|---|---|
| 看CSS语法 | 一天没睡 |
| 看HTML语法 | 还是没睡 |
| 看JS语法 | 还是GO香 |

---

<!-- Page 27 -->

# 添加动机-有序列表

- 王小二在添加了创造过程后，突然发现“发明前”还没写，于是用一个有序列表讲了讲自己写网页的目的。
  - 有序列表用一对 `<ol>...</ol>` 定义，
  - 列表每一项用一对 `<li>...</li>` 定义，
  - 一对 `<li>...</li>` 中间的文本是显示在列表项中的内容，
  - 有序列表在浏览器中显示时会自动出现序号
  - 把 `<ol>` 换成 `<ul>` 会定义无序列表（列表项前不显示序号）

```html
列表：王小二的动机
<ol>
    <li>学习CSS</li>
    <li>学习HTML</li>
    <li>学习JS</li>
    <li>写计算器和时钟</li>
</ol>
```

列表：王小二的动机

1. 学习CSS
2. 学习HTML
3. 学习JS
4. 写计算器和时钟

```html
<ol>
    <li>文本</li>
    <li>文本</li>
</ol>

---

<!-- Page 28 -->

# 添加计算器-表单

- 王小二在添加了写网页的目的后，想在页面上显示计算器，于是用下面关于表单的知识设计了计算器的样子
  - 表单是由一对 `<form>...</form>` 组成的元素，在这对标签中间可以使用**单一**的 `<input>` 定义各种各样的**表单元素**。
  - 表单通常用来获取用户的输入，表单内的表单元素通常是文本框或者选项，用户可以在文本框输入文本或者选择选项。
  - 各种表单元素使用 `<input>` 上的 **type 属性**来区分。

| 语法 | 描述 |
|---|---|
| `<input type="text">` | 能输入文本的框，实时显示用户输入的文本 |
| `<input type="password">` | 用来输入密码的框，将用户输入的字符显示为小圆点 |
| `<input type="radio" name="xx">` | 单选框，同一个表单多个中name属性相同的单选框只能选中一个 |
| `<input type="checkbox">` | 复选框 |
| `<input type="button" value="xx">` | 按钮（value属性的值为按钮上的文字） |

---

<!-- Page 29 -->

# 添加计算器-表单

- 王小二在添加了写网页的目的后，想在页面上显示计算器，于是用下面关于表单的知识设计了计算器的样子

```html
计算器：
<form>
    操作数1： <input type="text"><br/>
    操作数2： <input type="text">
    <br/>
    <input type="radio" name="op">加
    <input type="radio" name="op">减
    <input type="radio" name="op">乘
    <input type="radio" name="op">除
    <input type="radio" name="op">模
    <br/>
    <input type="button" value="计算">
</form>
```

一对 `<form>…</form>` 中同样 **name** 属性的单选框是一组，只能选中其一

## 发明后

终于做好了，虽然很粗糙，那也是亲儿子

计算器：

操作数1：  
操作数2：  

○ 加　○ 减　○ 乘　○ 除　○ 模

计算

- 注：现在只有计算器的样子，还没有定义功能。

---

<!-- Page 30 -->

# 3. 块级元素和内联元素

- 王小二正苦于不知道怎么写，不经意间发现了下面的知识。
  - 在网页中，有的HTML元素可以<span style="color: red;">嵌入在某一行</span>里，比如文本格式化标签`<i>`组成的元素，具有这种性质的元素称为**内联元素**
  - 反之，像由`<h1>`元素那样<span style="color: red;">以新的一行开始</span>的元素，称为**块级元素**
- 例子：
  - **`<div>…</div>`：**
    - 一个块级元素，无特殊含义。
    - 用于组合多个块级元素（一般网页比较复杂时可为网页分块），方便将来对多个元素进行整体操作。
  - **`<span>…</span>`：**
    - 一个内联元素，无特殊含义。
    - 用于组合多个行内元素或在某一行内嵌入元素，方便将来操作一行内的一部分。

---

<!-- Page 31 -->

# 代码分块-div 标签

- 王小二正好觉得代码有点乱，层次不够清楚，于是使用 `<div>` 标签把代码里发明三阶段分块，这样代码变得好看得多。
  - 加上 `<div>` 后只是改变了代码的结构，网页并没有什么变化。

```html
<body>
    ……
    <div>
        <h2>发明前</h2>
        ……
    </div>
    ……
    <div>
        <h2>发明后</h2>
        ……
    </div>
</body>

---

<!-- Page 32 -->

# 给计算器结果留位置— `span` 标签

- 王小二眉头一皱，发觉计算器并没有给计算结果留下位置，于是他使用 `<span>` 在 `<form>` 里加上结果的位置。

```html
计算器:
<form>
  操作数1： <input type="text"><br/>
  操作数2： <input type="text">
  <br/>
  <input type="radio" name="op">加
  <input type="radio" name="op">减
  <input type="radio" name="op">乘
  <input type="radio" name="op">除
  <input type="radio" name="op">模
  <br/>
  结果： <span>xxxx</span><br/>
  <input type="button" value="计算">
</form>
```

## 发明后

终于做好了，虽然很粗糙，那也是亲儿子

计算器:  
操作数1:  
操作数2:  
○加 ○减 ○乘 ○除 ○模  
结果: xxxx  
计算

---

<!-- Page 33 -->

# 添加元素的样式-方法一

- 王小二改完代码，开始思考怎样让自己的网页更漂亮，于是学习了下面有关 **CSS** 的知识：

  - **CSS**（Cascading Style Sheets，层叠样式表）是一种用来指定 **HTML** 元素的样式（如：字体颜色、背景颜色）的语言，其语句具有“<span style="color:red;">**名称：值；**</span>”的形式，HTML 文档有 <u>两种方法</u> 来加入 CSS 语句：

    - 方法一：直接在起始标签上定义 **style 属性**，属性的值是若干 CSS 语句。也可以同时规定不同方面的样式

  - 注意：**CSS** 语句中名称和值不是随便一个字符串，而是 CSS 语法里有的字符串

```html
<p style="background-color: gold;">一个金色背景的段落</p>
<p style="color:red;">一个红色字体的段落</p>

<h1 style="color:blue;text-align:center;">一个蓝色字体、居中的标题</h1>

---

<!-- Page 34 -->

# 给元素添加颜色

- 王小二给自己的标题、作者、时间添加了颜色

```html
<h1 style="background-color: gold;">计算器和时钟养成记</h1>
作者： <b><i style="color: purple;">王小二</i></b> <sup>*</sup>
时间： <i style="color: purple;">2020年02月26日</i>
```

美好的一天

# 计算器和时钟养成记

作者： ***王小二***<sup>*</sup> 时间： *2020年02月26日*

34

---

<!-- Page 35 -->

# 添加元素的样式-方法二

- 王小二觉得自己的网页还不够好看，想要把发明前、中、后各自美化一下，于是使用了下面的知识：
  - 方法二：在 `<head>...</head>` 中间定义 `<style>...</style>`，这对 `<style>` 内部是若干 CSS 语句

```html
<style>
    h1 {color:red;}  /* 规定某类标签的样式： 所有<h1>元素字体为红色 */
    #id属性的值 {color:green;}  /* 规定某个元素的样式： 被id属性【稍后讲述】指定的元素字体颜色为绿色 */
    .class属性的值 {color:yellow;} /* 规定某“组”元素的样式： 具有相同class属性【稍后讲述】的元素字体颜色为黄色 */
</style>

---

<!-- Page 36 -->

# 元素的通用属性

- 几乎所有的元素都可定义下面几种属性

| 属性 | 描述 |
|---|---|
| **id** | 元素的标识，不同元素需要不同id，可以是任意一个字符串 |
| **name** | 元素的名称，可以是任意一个字符串 |
| **class** | 元素的类，一个元素可以有若干个类，不同元素可以有相同类，可以是任意一个字符串 |
| **style** | 描述了元素的样式 |

```html
/* 定义属性的语法 */
<tag_name attribute1_name="…" attribute2_name="…" >…</tag_name>

---

<!-- Page 37 -->

# 给元素添加属性

- 只是加上`id`属性不会改变网页的显示效果，但是可以被`<style></style>`中间的语句选中，来添加网页的样式。

```html
<body>
    ……
    <div id="before">
        ……
    </div>
    <div id="during">
        ……
    </div>
    <div id="after">
        ……
    </div>
</body>

---

<!-- Page 38 -->

# 给元素添加颜色

- `#` 和 `id` 中间不能有空格

```html
<style>
  #before {
    background-color:skyblue;
  }
  #during {
    background-color:springgreen;
  }
  #after {
    background-color:tomato;
  }
  h2 {
    background-color:violet;
  }
</style>
```

# 计算器和时钟养成记

作者：王小二\* 时间：2020年02月26日

## 发明前

在发明前，我也不知道自己能不能做成。

列表：王小二的动机

1. 学习CSS
2. 学习HTML
3. 学习JS
4. 写计算器和时钟

## 发明中

过程中，查了好多资料，好累但很快乐。下表是我这几天的艰辛历程：

表：王小二艰难历程表

| 事件 | 感悟 |
|---|---|
| 看CSS语法 | 一天没睡 |
| 看HTML语法 | 还是没睡 |
| 看JS语法 | 还是GO香 |

## 发明后

终于做好了，虽然很粗糙，那也是亲儿子

计算器：

操作数1：  
操作数2：  

○ 加　○ 减　● 乘　○ 除　○ 模

结果：xxxx

[计算]

---

<!-- Page 39 -->

# 课堂小测验

- 下列属性以及描述中错误的是：

  - **A.** 不同的 `HTML` 元素可以有相同的 `id` 属性
  - **B.** 不同的 `HTML` 元素可以有相同的 `class` 属性
  - **C.** 不同的 `HTML` 元素可以有相同的 `name` 属性
  - **D.** `HTML` 元素的 `style` 属性规定了元素的样式

[使用电脑的同学可点此链接]()

39

---

<!-- Page 40 -->

# 4. 交流讨论

- 请开始构思与创作你的作品
  - 作品的名称
  - 作品的主要内容

---

<!-- Page 41 -->

# <span style="color:red">课程追求： 珍惜时代、体认思维、学生走心、作品牵引</span>

## <span style="color:#2b0055">李晓明 徐志伟 | 大湾区大学</span>